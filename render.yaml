services:
  - type: web
    name: aider-agent
    runtime: python
    plan: free
    buildCommand: echo "ready"
    startCommand: python server.py
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: GITHUB_PAT
        sync: false
